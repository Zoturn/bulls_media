# Generated output. Keep this list in agreement with .gitignore and the globalIgnores in
# eslint.config.mjs — `npm run format` runs over the whole repo, so anything generated that is
# missing here gets reformatted.
.next
out
build
node_modules
coverage
src/generated
cypress/screenshots
cypress/videos
cypress/downloads
cypress/fixtures
prisma/migrations
package-lock.json
openspec/specs

# Rule files are authored to a fixed house format; Prettier rewrites their frontmatter quoting.
.claude/rules
